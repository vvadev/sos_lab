{
  "port": "env(PORT)",
  "settings": {
    "loggerFormat": "foal",
    "session": {
      "store": "@foal/typeorm"
    }
  },
  "database": {
    "type": "postgres",
    "host": "env(DB_HOST)",
    "port": "env(DB_PORT)",
    "username": "env(DB_USERNAME)",
    "password": "env(DB_PASSWORD)",
    "database": "env(DB_NAME)"
  }
}